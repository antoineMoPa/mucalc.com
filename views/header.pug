header
  col-sm-6
    img(src="/images/logo.png").header-logo
    h1.inline-block
      a(href='https://livecalc.xyz') livecalc.xyz
  col-sm-6.text-right.links        
    a.new-sheet-button(href='/new')
      button New Sheet
    if(page == "sheet")
      .icon.burger-icon.inline-block
  clearfix
.main-nav
  include ./nav.pug
