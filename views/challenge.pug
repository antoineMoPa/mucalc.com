.new-challenge
  form(action="/challenge", method="post")
    h1 New Challenge
    fieldset
      label Challenge title
      p.info What is this challenge about?
      input(
          name="title", placeholder="Enter title",
          value=(title || "")
          ).title-input
    fieldset
      label Your question
      p.info What is the result you want?
      textarea(
          name="question", placeholder="Enter a question", 
          value=(question||""))
    fieldset
      label Time
      p.info How many seconds do players have to solve the challenge?
      input(name="time", type="number", value=(time||"10"))
    fieldset
      label Initial sheet content
      p.info Give players something to start with
      livecalc.initial-content(data-value=(initial_content||""))
    fieldset
      label Answer validation expression
      p.info Write a expression that returns true or false. `ans` is the cell result. For example, if the only acceptable answer is 42, type: ans == 42
      livecalc.validator(data-value=(validator)||"")
    .text-center
      button.submit-new-challenge
        |  Sumbit
